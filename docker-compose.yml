# Run the bot on a server (e.g. GCP VM) with: docker compose up -d --build
# It auto-restarts on crash/reboot and reads secrets from .env.
services:
  bot:
    build: .
    restart: unless-stopped
    env_file: .env
    ports:
      - "8080:8080"
